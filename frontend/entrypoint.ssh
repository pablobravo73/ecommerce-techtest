#!/bin/sh

# Verificar si la variable de entorno NODE_ENV está establecida en "production"
if [ "$NODE_ENV" = "production" ]; then
  echo "Starting application in production mode"
  node ./dist/server.js
else
  echo "Starting application in development mode"
  yarn dev
fi
